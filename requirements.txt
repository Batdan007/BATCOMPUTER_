# Core UI & ML stack
gradio>=4.0
torch>=2.2
numpy>=1.26          # WAN pins numpy<2, so we ask for the newest 1.x series
Pillow>=10.0
# The [spaces] extra gives us the spaces module used in app.py
huggingface-hub[spaces]>=0.33.5

# Optional development helpers (kept for completeness)
autopep8>=2.0
black>=23.0
flake8>=6.0
pygments>=2.15
pathlib2>=2.3.7
